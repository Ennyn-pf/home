---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Thesis — Ennyn"
  description="The two stacks and the seams between them. An orchestration map for the SMB back office."
  register="b"
  activeNav="thesis"
>
  <main class="thesis">

    <!-- HERO -->
    <header class="hero">
      <div class="wrap-mid">
        <p class="kicker">The Ennyn Thesis · v1</p>
        <h1>The two stacks &mdash; and the <em>seams</em> between them.</h1>
        <p class="lede">
          Every SMB runs on two parallel software worlds that don't talk to each other.
          The system-of-record vendors own the inside of each box and are racing to ship
          their own agents there. The territory <em>between</em> the boxes is the only
          place nobody owns &mdash; and it's where the company actually runs.
        </p>
      </div>
    </header>

    <!-- 01: THE TWO STACKS -->
    <section>
      <div class="wrap-mid">
        <div class="sec-head">
          <span class="sec-num">01</span>
          <div>
            <h2>The operator runs on two disconnected worlds.</h2>
            <p class="lede">
              Everything an SMB touches falls into one of two stacks. The system-of-record
              vendors own the inside of each box. The lightning bolt between them is the
              only territory nobody owns.
            </p>
          </div>
        </div>

        <div class="stacks-grid">
          <div class="stack stack-a">
            <p class="stack-title">Stack A · Operations</p>
            <ul>
              <li>The unit of work <span class="mono">AppFolio · Buildium · Jobber · ServiceTitan · Mindbody</span></li>
              <li>Inbound leads &amp; scheduling <span class="mono">Calendly · Typeform · Yelp · Zillow</span></li>
              <li>Service delivery &amp; dispatch <span class="mono">Housecall Pro · Vagaro · Square Appointments</span></li>
              <li>Customer portals &amp; billing <span class="mono">Stripe · Square · Recurly</span></li>
              <li>Reporting &amp; analytics <span class="mono">Google Sheets · QuickBooks reports · Metabase</span></li>
            </ul>
          </div>
          <div class="gap">
            <span class="gap-bolt">⚡</span>
            <span class="gap-label">The orchestration seam</span>
          </div>
          <div class="stack stack-b">
            <p class="stack-title">Stack B · People &amp; business</p>
            <ul>
              <li>Payroll, HR, benefits <span class="mono">Gusto · ADP · OnPay</span></li>
              <li>Accounting <span class="mono">QuickBooks · Xero</span></li>
              <li>Email and docs <span class="mono">Google · Microsoft 365</span></li>
              <li>Hiring &amp; ATS <span class="mono">Indeed · LinkedIn</span></li>
              <li>Marketing &amp; brand <span class="mono">LinkedIn · Instagram · Mailchimp</span></li>
              <li>Lead generation <span class="mono">Google Ads · Meta Ads · listing sites</span></li>
              <li>Training and supervision <span class="mono warn">⚠ mostly none</span></li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- 02: THE THREE LOOPS -->
    <section>
      <div class="wrap-mid">
        <div class="sec-head">
          <span class="sec-num">02</span>
          <div>
            <h2>Stack A resolves into a few repeating revenue loops.</h2>
            <p class="lede">
              The operations side isn't flat work &mdash; it concentrates onto a handful of
              repeating loops, each with a hard dollar value. This is where the operator's
              money and time both live. Three loops recur across nearly every SMB &mdash;
              acquisition, satisfaction, and the long middle of steady-state delivery.
            </p>
          </div>
        </div>

        <div class="loops">
          <article class="loop">
            <p class="loop-tag">Loop 01 · Acquisition</p>
            <h3>Win the customer.</h3>
            <p>A prospect raises a hand &mdash; paid ad, search, listing site, referral. The clock is opportunity lost per day. Metric: <b>speed-to-close</b>.</p>
            <p class="loop-roi">$ = days of revenue lost × daily margin</p>
          </article>
          <article class="loop">
            <p class="loop-tag">Loop 02 · Satisfaction</p>
            <h3>Earn the next engagement.</h3>
            <p>The relationship is the asset. Every save, every quietly-resolved issue, every NPS-9 conversation compounds. Highest-ROI loop in most SMBs. Metric: <b>retention rate</b>.</p>
            <p class="loop-roi">$ = re-acquisition cost saved + lifetime extended</p>
          </article>
          <article class="loop">
            <p class="loop-tag">Loop 03 · Steady state</p>
            <h3>Run the day-to-day.</h3>
            <p>The long middle. High volume, low judgment per event &mdash; but constant. Metric: <b>cost-to-serve</b>.</p>
            <p class="loop-roi">$ = labor hours per customer per month</p>
          </article>
        </div>
      </div>
    </section>

    <!-- 03: THE SEAMS -->
    <section>
      <div class="wrap-mid">
        <div class="sec-head">
          <span class="sec-num">03</span>
          <div>
            <h2>The seams &mdash; where the handoffs break.</h2>
            <p class="lede">
              Each seam is a place where work has to jump between Stack A and Stack B
              (or between two tools in the same stack) and today gets done by a human
              copying data, remembering a step, or dropping it. This is the agentic
              target list.
            </p>
          </div>
        </div>

        <div class="seams">
          <div class="seam priority">
            <span class="seam-id">S1</span>
            <div>
              <h4>New hire → fully provisioned operator <span class="badge hi">Highest value</span></h4>
              <p class="flow"><b>Stack B</b> hired in Gusto &nbsp;⚡&nbsp; <b>Stack A</b> account, role, access, training plan</p>
              <p>Broken today: someone manually creates the new rep in the ops system, sets permissions, assigns territory, emails credentials. Days of lag; steps get missed.</p>
            </div>
          </div>
          <div class="seam priority">
            <span class="seam-id">S2</span>
            <div>
              <h4>Operator performance → coaching <span class="badge hi">Whitespace</span></h4>
              <p class="flow"><b>Stack A</b> outcomes (speed-to-close, conversion) &nbsp;⚡&nbsp; <b>Stack B</b> supervision (the loop with no incumbent)</p>
              <p>Broken today: nobody connects how fast or well each operator works the acquisition loop to any coaching. Performance lives in the ops system; "management" lives nowhere.</p>
            </div>
          </div>
          <div class="seam">
            <span class="seam-id">S3</span>
            <div>
              <h4>Work order → vendor pay → books <span class="badge med">High value</span></h4>
              <p class="flow"><b>Stack A</b> work order completed &nbsp;⚡&nbsp; <b>Stack B</b> QuickBooks vendor bill, allocation, reconciliation</p>
              <p>Broken today: work order closes in the ops system; someone re-keys the invoice into QuickBooks, allocates it to the right property/owner, and reconciles. Double entry, lag, errors.</p>
            </div>
          </div>
          <div class="seam">
            <span class="seam-id">S4</span>
            <div>
              <h4>Field hours → payroll reconciliation <span class="badge med">Medium</span></h4>
              <p class="flow"><b>Stack A</b> hours logged on jobs &nbsp;⚡&nbsp; <b>Stack B</b> Gusto payroll &amp; PTO</p>
              <p>Broken today: hourly crews log time against jobs in one place and get paid from another; reconciling and PTO is manual.</p>
            </div>
          </div>
          <div class="seam">
            <span class="seam-id">S5</span>
            <div>
              <h4>Documents → e-signature → record <span class="badge med">In-stack glue</span></h4>
              <p class="flow"><b>Stack A</b> document generated &nbsp;⚡&nbsp; DocuSign &nbsp;⚡&nbsp; <b>Stack A</b> filed + status updated</p>
              <p>Broken today: the e-signature round-trip and refiling the executed doc + flipping status is manual chasing.</p>
            </div>
          </div>
          <div class="seam">
            <span class="seam-id">S6</span>
            <div>
              <h4>Customer-acquisition → onboarding <span class="badge med">Growth loop</span></h4>
              <p class="flow"><b>Stack B</b> marketing campaign &nbsp;⚡&nbsp; <b>Stack A</b> new customer record + initial setup</p>
              <p>Broken today: winning a new customer means manually creating the record, configuring access, setting up reporting &mdash; the slowest part of growth.</p>
            </div>
          </div>
        </div>

        <p class="seam-extend">
          These six are the canonical seam pattern across SMB verticals. The same shape
          extends naturally to every other Stack B surface:
        </p>
        <ul class="seam-extend-list">
          <li>Instagram tenant DMs landing as tour requests in the ops platform</li>
          <li>LinkedIn B2B outreach feeding the new-owner pipeline with full attribution</li>
          <li>Listing-site and Google Ads leads routed into the ops stack &mdash; spend tied directly to revenue outcomes</li>
          <li>Brand-building content scheduled across channels, scored against the metrics that justify the spend</li>
        </ul>

        <p class="seam-extend">
          <b>And one more:</b> compliance and governance are seams too &mdash; sometimes the
          most expensive ones. Every Stack A ↔ Stack B handoff is also a governance event.
          Who authorized the charge? Who provisioned the access? Who signed off on the
          customer decision? Today the audit trail for "who did what, with what authority"
          is scattered across four vendors' logs &mdash; when those logs exist at all.
        </p>
        <ul class="seam-extend-list">
          <li>Single audit trail for cross-stack handoffs, not four scattered ones</li>
          <li>Access provisioning on hire; clean revocation on departure</li>
          <li>Approval signoffs for money-out actions above threshold, kept in one record</li>
          <li>Industry-specific carve-outs (Fair Housing, HIPAA, GLBA, licensing) wired into the autonomy bands &mdash; see Section 04</li>
          <li>Contractor classification and insurance verification tracked across vendors</li>
        </ul>

        <p class="seam-extend-note">
          The seams are universal &mdash; the carve-outs are local. Fair Housing in property
          management. HIPAA in healthcare. GLBA in finance. Food-safety and wage-and-hour
          across hospitality. OSHA in trades. <a href={`${base}/industries`}>See your vertical →</a>
        </p>

        <p class="seam-extend-close">Different surfaces. Different stakes. Same seams.</p>
      </div>
    </section>

    <!-- 04: AUTONOMY LINE -->
    <section>
      <div class="wrap-mid">
        <div class="sec-head">
          <span class="sec-num">04</span>
          <div>
            <h2>Where to draw the human-in-the-loop line.</h2>
            <p class="lede">
              "Capable of anything" is a liability until constrained. Every seam step
              gets sorted into one of three columns. Early pilots keep the leash short
              &mdash; autonomy is earned, not granted.
            </p>
          </div>
        </div>

        <div class="autonomy">
          <div class="auto-col green">
            <h5>● Autonomous</h5>
            <p class="auto-sub">Retrieval, logging, drafting, reconciliation surfacing</p>
            <ul>
              <li>Pull metrics &amp; build digests</li>
              <li>Detect expirations and triggers</li>
              <li>Reconcile hours vs. pay, surface mismatches</li>
              <li>Chase unsigned docs on cadence</li>
              <li>Draft vendor bills coded to property</li>
            </ul>
          </div>
          <div class="auto-col amber">
            <h5>◐ Draft → approve</h5>
            <p class="auto-sub">Anything client- or money-facing</p>
            <ul>
              <li>Renewal offers &amp; pricing</li>
              <li>Coaching nudges to operators</li>
              <li>Payroll run before submit</li>
              <li>Owner kickoff sequences</li>
              <li>Vendor payments over threshold</li>
            </ul>
          </div>
          <div class="auto-col red">
            <h5>○ Human only</h5>
            <p class="auto-sub">Legal exposure or irreversible</p>
            <ul>
              <li>Customer/applicant decisions under protected-class logic (Fair Housing, ADA, fair hiring)</li>
              <li>HIPAA / PHI disclosure and equivalents</li>
              <li>Termination, eviction, or discipline decisions</li>
              <li>Granting system access</li>
              <li>Money-out actions above approval threshold</li>
              <li>Final hire decisions</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- CLOSING / STRATEGIC NOTE -->
    <section class="closing">
      <div class="wrap-narrow">
        <div class="sec-head">
          <span class="sec-num">05</span>
          <div>
            <h2>The strategic shape.</h2>
          </div>
        </div>
        <p>
          Inside each box, the system-of-record vendors are already shipping agents.
          Realm-X handles the PM silo. ServiceTitan IQ handles the trades silo. Toast,
          Mindbody, and every other vertical SoR are shipping their own in-silo AI.
          The all-in-one HR platforms automate the people stack. <b>A frontal assault on
          either box loses.</b>
        </p>
        <p>
          The defensible position is the <em>between</em> &mdash; the cross-stack seams
          where each vendor sees only its own silo and none is incentivized to build the
          bridge. Plug <em>into</em> their marketplaces (AppFolio Stack, ServiceTitan
          Marketplace, Toast Marketplace &mdash; every SoR has a developer surface) as
          connective tissue. Don't replace the system of record.
        </p>
        <p>
          That's Ennyn.
        </p>
      </div>
    </section>

    <!-- INDUSTRIES BRIDGE — five concrete entrypoints -->
    <section class="thesis-bridge">
      <div class="wrap-narrow">
        <div class="sec-head">
          <span class="sec-num">06</span>
          <div>
            <h2>See it in your business.</h2>
            <p class="lede">
              Same orchestration shape, different tools and language. Pick a vertical
              and read the concrete shape.
            </p>
          </div>
        </div>

        <ol class="bridge-list">
          <li class="bridge-item accent-rust">
            <a href={`${base}/property-management`}>
              <span class="bridge-num">01</span>
              <div class="bridge-body">
                <h3>Property management</h3>
                <p class="bridge-tools">AppFolio · Buildium · 50–500 doors</p>
              </div>
              <span class="bridge-arrow">→</span>
            </a>
          </li>
          <li class="bridge-item accent-gold">
            <a href={`${base}/fitness`}>
              <span class="bridge-num">02</span>
              <div class="bridge-body">
                <h3>Fitness studios &amp; gyms</h3>
                <p class="bridge-tools">Mindbody · Glofox · 200–500 members</p>
              </div>
              <span class="bridge-arrow">→</span>
            </a>
          </li>
          <li class="bridge-item accent-slate">
            <a href={`${base}/trades`}>
              <span class="bridge-num">03</span>
              <div class="bridge-body">
                <h3>Home services &amp; trades</h3>
                <p class="bridge-tools">ServiceTitan · Jobber · 5–25 techs</p>
              </div>
              <span class="bridge-arrow">→</span>
            </a>
          </li>
          <li class="bridge-item accent-moss">
            <a href={`${base}/medical`}>
              <span class="bridge-num">04</span>
              <div class="bridge-body">
                <h3>Dental &amp; small medical</h3>
                <p class="bridge-tools">Dentrix · ZocDoc · 2–10 chair</p>
              </div>
              <span class="bridge-arrow">→</span>
            </a>
          </li>
          <li class="bridge-item accent-plum">
            <a href={`${base}/restaurants`}>
              <span class="bridge-num">05</span>
              <div class="bridge-body">
                <h3>Restaurant &amp; café groups</h3>
                <p class="bridge-tools">Toast · Square · 2–6 locations</p>
              </div>
              <span class="bridge-arrow">→</span>
            </a>
          </li>
        </ol>

        <p class="bridge-foot">
          Or <a href={`${base}/industries`}>see all five at a glance →</a>
        </p>
      </div>
    </section>

  </main>
</BaseLayout>

<style>
  .thesis section {
    padding: 80px 0;
    border-bottom: 1px solid var(--line);
  }
  .thesis .hero {
    padding: 120px 0 64px;
    border-bottom: 3px solid var(--ink);
  }
  .thesis .kicker { margin-bottom: 18px; }
  .thesis h1 { max-width: 14ch; margin-bottom: 22px; }
  .thesis .lede { font-family: 'EB Garamond', serif; }

  .thesis .sec-head {
    display: flex;
    gap: 18px;
    align-items: baseline;
    margin-bottom: 36px;
  }
  .thesis .sec-head > div { flex: 1; }
  .thesis .sec-head h2 { margin-bottom: 12px; }
  .thesis .sec-head .lede { font-size: 17px; max-width: 70ch; }

  /* Two stacks */
  .thesis .stacks-grid {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 0;
    align-items: stretch;
  }
  .thesis .stack {
    border: 2px solid var(--ink);
    background: rgba(255,255,255,0.45);
  }
  .thesis .stack-a { border-color: var(--slate); }
  .thesis .stack-b { border-color: var(--moss); }
  .thesis .stack-title {
    padding: 14px 18px;
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    font-weight: 600;
    color: #fff;
    margin: 0;
  }
  .thesis .stack-a .stack-title { background: var(--slate); }
  .thesis .stack-b .stack-title { background: var(--moss); }
  .thesis .stack ul { list-style: none; }
  .thesis .stack li {
    padding: 13px 18px;
    border-top: 1px solid var(--line);
    font-family: 'EB Garamond', serif;
    font-size: 16px;
    display: flex;
    justify-content: space-between;
    gap: 10px;
    align-items: center;
    flex-wrap: wrap;
  }
  .thesis .stack .mono {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 11px;
    color: var(--soft);
    background: var(--paper-2);
    padding: 2px 7px;
    border-radius: 2px;
    white-space: nowrap;
  }
  .thesis .stack .warn { color: var(--rust); }
  .thesis .gap {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 0 22px;
  }
  .thesis .gap-bolt {
    font-size: 30px;
    color: var(--rust);
    margin: 14px 0;
  }
  .thesis .gap-label {
    writing-mode: vertical-rl;
    text-orientation: mixed;
    font-family: 'IBM Plex Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--rust);
    font-weight: 600;
    transform: rotate(180deg);
  }

  /* Loops */
  .thesis .loops {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 18px;
  }
  .thesis .loop {
    border: 2px solid var(--ink);
    padding: 22px 22px 26px;
    background: rgba(255, 255, 255, 0.5);
    position: relative;
    overflow: hidden;
  }
  .thesis .loop::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 6px;
    background: var(--rust);
  }
  .thesis .loop:nth-child(2)::before { background: var(--moss); }
  .thesis .loop:nth-child(3)::before { background: var(--slate); }
  .thesis .loop-tag {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 10.5px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--rust);
    font-weight: 600;
    margin-bottom: 8px;
  }
  .thesis .loop:nth-child(2) .loop-tag { color: var(--moss); }
  .thesis .loop:nth-child(3) .loop-tag { color: var(--slate); }
  .thesis .loop h3 {
    font-size: 22px;
    margin-bottom: 10px;
    line-height: 1.1;
  }
  .thesis .loop p { font-size: 15px; margin-bottom: 12px; }
  .thesis .loop-roi {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 11px;
    background: var(--ink);
    color: var(--paper);
    padding: 8px 11px;
    border-radius: 2px;
    letter-spacing: 0.04em;
    margin-top: 12px;
    display: inline-block;
  }

  /* Seams */
  .thesis .seams { margin-top: 8px; }
  .thesis .seam {
    border: 1px solid var(--line);
    border-left: 5px solid var(--rust);
    background: rgba(255, 255, 255, 0.55);
    padding: 22px 24px;
    margin-bottom: 14px;
    display: grid;
    grid-template-columns: 36px 1fr;
    gap: 18px;
    align-items: start;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }
  .thesis .seam:hover {
    transform: translateX(4px);
    box-shadow: -6px 6px 0 var(--shadow);
  }
  .thesis .seam.priority {
    border-left-color: var(--moss);
    background: rgba(79, 107, 58, 0.06);
  }
  .thesis .seam-id {
    font-family: 'Fraunces', serif;
    font-size: 32px;
    font-weight: 900;
    color: var(--rust);
    line-height: 1;
  }
  .thesis .seam.priority .seam-id { color: var(--moss); }
  .thesis .seam h4 {
    font-family: 'Fraunces', serif;
    font-size: 18px;
    font-weight: 700;
    margin-bottom: 4px;
    color: var(--ink);
  }
  .thesis .seam .flow {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12px;
    color: var(--slate);
    margin-bottom: 10px;
    font-weight: 500;
  }
  .thesis .seam .flow b { color: var(--rust); }
  .thesis .seam p {
    font-size: 15px;
    color: var(--muted);
    line-height: 1.55;
  }
  .thesis .badge {
    display: inline-block;
    font-family: 'IBM Plex Mono', monospace;
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 3px 8px;
    border-radius: 2px;
    font-weight: 600;
    margin-left: 8px;
  }
  .thesis .badge.hi { background: var(--moss); color: #fff; }
  .thesis .badge.med { background: var(--gold); color: #fff; }

  /* Autonomy */
  .thesis .autonomy {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
    border: 2px solid var(--ink);
  }
  .thesis .auto-col {
    padding: 22px 20px;
    border-right: 1px solid var(--line);
  }
  .thesis .auto-col:last-child { border-right: none; }
  .thesis .auto-col h5 {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 4px;
    font-weight: 600;
  }
  .thesis .auto-col.green { background: rgba(79, 107, 58, 0.08); }
  .thesis .auto-col.green h5 { color: var(--moss); }
  .thesis .auto-col.amber { background: rgba(189, 138, 44, 0.1); }
  .thesis .auto-col.amber h5 { color: #946a18; }
  .thesis .auto-col.red { background: rgba(180, 71, 31, 0.08); }
  .thesis .auto-col.red h5 { color: var(--rust); }
  .thesis .auto-sub {
    font-family: 'Fraunces', serif;
    font-size: 13px;
    color: var(--soft);
    margin-bottom: 14px;
    font-style: italic;
  }
  .thesis .auto-col ul { list-style: none; }
  .thesis .auto-col li {
    padding: 7px 0;
    border-top: 1px dotted var(--line);
    font-family: 'EB Garamond', serif;
    font-size: 15px;
  }
  .thesis .auto-col li:first-of-type { border-top: none; }

  .thesis .closing { background: var(--paper-2); }
  .thesis .closing p { margin-bottom: 18px; }
  .thesis .closing em { font-style: italic; color: var(--rust); }

  .thesis .seam-extend {
    margin-top: 28px;
    padding: 20px 24px 14px;
    border-left: 3px solid var(--gold);
    background: rgba(189, 138, 44, 0.05);
    font-family: 'Fraunces', serif;
    font-size: 16px;
    font-style: italic;
    color: var(--muted);
    max-width: none;
    line-height: 1.6;
  }
  .thesis .seam-extend-list {
    list-style: none;
    padding: 0 24px 14px;
    border-left: 3px solid var(--gold);
    background: rgba(189, 138, 44, 0.05);
    margin-top: -8px;
  }
  .thesis .seam-extend-list li {
    font-family: 'EB Garamond', serif;
    font-size: 16px;
    color: var(--muted);
    padding: 6px 0 6px 22px;
    position: relative;
    line-height: 1.55;
  }
  .thesis .seam-extend-list li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--gold);
    font-weight: 700;
  }
  .thesis .seam-extend-close {
    padding: 14px 24px 20px;
    border-left: 3px solid var(--gold);
    background: rgba(189, 138, 44, 0.05);
    margin-top: -8px;
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--rust);
    font-weight: 600;
    max-width: none;
  }
  .thesis .seam-extend-note {
    padding: 14px 24px;
    border-left: 3px solid var(--gold);
    background: rgba(189, 138, 44, 0.05);
    margin-top: -8px;
    font-family: 'EB Garamond', serif;
    font-style: italic;
    font-size: 15.5px;
    color: var(--muted);
    line-height: 1.6;
    max-width: none;
  }
  .thesis .seam-extend-note a {
    color: var(--ink);
    text-decoration: none;
    border-bottom: 1px solid var(--ink);
    font-style: normal;
    font-weight: 600;
    padding-bottom: 1px;
    transition: color 0.15s ease, border-color 0.15s ease;
  }
  .thesis .seam-extend-note a:hover {
    color: var(--rust);
    border-color: var(--rust);
  }

  /* === INDUSTRIES BRIDGE === */
  .thesis .thesis-bridge {
    background: var(--paper-2);
    padding: 96px 0;
  }
  .thesis .bridge-list {
    list-style: none;
    padding: 0;
    margin: 0;
    border-top: 1px solid var(--line);
  }
  .thesis .bridge-item {
    border-bottom: 1px solid var(--line);
    border-left: 4px solid transparent;
    transition: background 0.15s ease, border-left-color 0.15s ease;
  }
  .thesis .bridge-item.accent-rust  { border-left-color: var(--rust); }
  .thesis .bridge-item.accent-gold  { border-left-color: var(--gold); }
  .thesis .bridge-item.accent-slate { border-left-color: var(--slate); }
  .thesis .bridge-item.accent-moss  { border-left-color: var(--moss); }
  .thesis .bridge-item.accent-plum  { border-left-color: var(--plum); }
  .thesis .bridge-item:hover { background: rgba(255, 255, 255, 0.55); }
  .thesis .bridge-item a {
    display: grid;
    grid-template-columns: 44px 1fr auto;
    gap: 18px;
    align-items: center;
    padding: 22px 24px;
    text-decoration: none;
    color: var(--ink);
  }
  .thesis .bridge-num {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 13px;
    color: var(--rust);
    font-weight: 600;
    letter-spacing: 0.1em;
  }
  .thesis .bridge-body h3 {
    font-family: 'Fraunces', serif;
    font-size: 22px;
    font-weight: 700;
    letter-spacing: -0.01em;
    margin-bottom: 4px;
    color: var(--ink);
    line-height: 1.15;
  }
  .thesis .bridge-tools {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12px;
    color: var(--soft);
    letter-spacing: 0.04em;
    margin: 0;
    max-width: none;
  }
  .thesis .bridge-arrow {
    font-family: 'Fraunces', serif;
    font-size: 24px;
    color: var(--ink);
    font-weight: 600;
    transition: transform 0.15s ease;
  }
  .thesis .bridge-item:hover .bridge-arrow {
    transform: translateX(4px);
  }
  .thesis .bridge-foot {
    margin: 32px auto 0;
    text-align: center;
    max-width: none;
    font-family: 'EB Garamond', serif;
    font-style: italic;
    font-size: 16px;
    color: var(--muted);
  }
  .thesis .bridge-foot a {
    color: var(--ink);
    text-decoration: none;
    border-bottom: 1px solid var(--ink);
    font-style: normal;
    font-weight: 600;
    padding-bottom: 1px;
  }
  .thesis .bridge-foot a:hover {
    color: var(--rust);
    border-color: var(--rust);
  }

  @media (max-width: 860px) {
    .thesis .stacks-grid { grid-template-columns: 1fr; }
    .thesis .gap { flex-direction: row; padding: 18px 0; }
    .thesis .gap-label { writing-mode: horizontal-tb; transform: none; }
    .thesis .gap-bolt { margin: 0 14px; }
    .thesis .loops { grid-template-columns: 1fr; }
    .thesis .autonomy { grid-template-columns: 1fr; }
    .thesis .auto-col { border-right: none; border-bottom: 1px solid var(--line); }
  }
</style>
